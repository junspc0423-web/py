def exam3(order_list) :
  
  menu= {"누가바":800,"돼지바":600,"바밤바":900,"보석바":400,"메로나":400,"쌍쌍바":700}
  
  total_price=0

  for i in order_list :
    price = menu[i]
    num = order_list[i]

    total_price = total_price + (price*num)
  return total_price

exam3({"누가바": 2, "쌍쌍바": 1}) 